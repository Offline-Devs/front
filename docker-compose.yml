services:
  web:
    build: .
    environment:
      - NODE_ENV=production
    expose:
      - "3000"
  nginx:
    image: nginx:1.27-alpine
    depends_on:
      - web
    ports:
      - "80:80"
    volumes:
      - ./nginx/nginx.conf:/etc/nginx/nginx.conf:ro
