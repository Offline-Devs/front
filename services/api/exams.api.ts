import { apiRequest } from "./client"; import type { Exam, ExamInput, ExamUpdateInput } from "@/types/exam"; import type { StatusResponse } from "@/types/api";
// CRUD آزمون؛ PUT subjects را در بک‌اند به‌طور کامل replace می‌کند.
export const examsApi = { list: () => apiRequest<Exam[]>("/exams"), get: (id: string) => apiRequest<Exam>(`/exams/${id}`), create: (input: ExamInput) => apiRequest<Exam>("/exams", { method: "POST", body: JSON.stringify(input) }), update: (id: string, input: ExamUpdateInput) => apiRequest<Exam>(`/exams/${id}`, { method: "PUT", body: JSON.stringify(input) }), remove: (id: string) => apiRequest<StatusResponse>(`/exams/${id}`, { method: "DELETE" }) };
