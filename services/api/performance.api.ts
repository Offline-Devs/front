import { apiRequest } from "./client"; import type { PerformanceHistory, PerformanceInput, PerformanceUpdateInput } from "@/types/performance"; import type { StatusResponse } from "@/types/api";
// دانش‌آموز فقط خواننده performance است؛ create/update/delete فقط در namespace مدیر قرار دارند.
export const performanceApi = { mine: () => apiRequest<PerformanceHistory[]>("/students/performance"), forStudent: (id: string) => apiRequest<PerformanceHistory[]>(`/admin/students/${id}/performance`), create: (id: string, input: PerformanceInput) => apiRequest<PerformanceHistory>(`/admin/students/${id}/performance`, { method: "POST", body: JSON.stringify(input) }), update: (id: string, input: PerformanceUpdateInput) => apiRequest<StatusResponse>(`/admin/performance/${id}`, { method: "PUT", body: JSON.stringify(input) }), remove: (id: string) => apiRequest<StatusResponse>(`/admin/performance/${id}`, { method: "DELETE" }) };
